name: Stale Issue Cron
on:
  schedule:
    # Every day at midnight
    - cron: "0 0 * * *"
  workflow_dispatch:
jobs:
  stale:
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: actions/stale@v5.1.0
