name: Setup sqlc
description: |
  Sets up the sqlc environment for tests, builds, etc.
runs:
  using: "composite"
  steps:
    - name: Setup sqlc
      uses: sqlc-dev/setup-sqlc@v4
      with:
        sqlc-version: "1.20.0"
