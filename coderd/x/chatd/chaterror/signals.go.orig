diff --git a/coderd/x/chatd/chaterror/signals.go b/coderd/x/chatd/chaterror/signals.go
index eb09a6d62..b6dd9bbba 100644
--- a/coderd/x/chatd/chaterror/signals.go
+++ b/coderd/x/chatd/chaterror/signals.go
@@ -37,6 +37,14 @@ var (
 		"broken pipe",
 		"bad gateway",
 		"gateway timeout",
+		// HTTP/2 transport-layer failures (force-closed streams,
+		// GOAWAY, etc). Classify as transient so the retry loop
+		// drives and the user sees a per-provider message.
+		"force closed",
+		"clientconn.close",
+		"goaway",
+		"http2: stream closed",
+		"use of closed network connection",
 	}
 	authStrongPatterns = []string{
 		"authentication",
