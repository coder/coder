/**
 * Global setup for our Jest tests
 */

// Set up 'next-router-mock' to with our tests:
// jest.mock('next/router', () => require('next-router-mock'))
jest.mock("next/router", () => require("next-router-mock"))
