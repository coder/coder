package validate

// Numeric returns true if s contains only digits.
// Returns false otherwise.
func Numeric(s string) bool {
	if s == "" {
		return false
	}
	for _, r := range s {
		if r < '0' || r > '9' {
			return false
		}
	}
	return true
}
