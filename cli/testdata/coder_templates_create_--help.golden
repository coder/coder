Usage: coder templates create [name]

Create a template from the current directory or as specified by flag

Options
    --parameter-file
        Specify a file path with parameter values.

    --variables-file
        Specify a file path with values for Terraform-managed variables.

    --variable
        Specify a set of values for Terraform-managed variables.

    --provisioner-tag
        Specify a set of tags to target provisioner daemons.

    --default-ttl (default: 24h) 
        Specify a default TTL for workspaces created from this template.

    --directory, -d (default: .) 
        Specify the directory to create from, use '-' to read tar from stdin

    --yes, -y
        Bypass prompts.

---
Report bugs and request features at https://github.com/coder/coder/issues/new
