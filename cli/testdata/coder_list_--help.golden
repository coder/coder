Usage: coder list

List workspaces

Options
    --all, -a (default: false) 
        Specifies whether all workspaces will be listed or not.

    --search (default: owner:me) 
        Search for a workspace with a query.

    --column, -c (default: workspace,template,status,last built,outdated,starts at,stops after) 
        Columns to display in table output. Available columns: workspace,
        template, status, last built, outdated, starts at, stops after

    --output, -o (default: table) 
        Output format. Available formats: table, json

---
Report bugs and request features at https://github.com/coder/coder/issues/new
