Usage: coder state push <workspace> <file>

Push a Terraform state file to a workspace.

Options
    --build, -b (default: 0) 
        Specify a workspace build to target by name.

---
Run `coder --help` for a list of global options.
