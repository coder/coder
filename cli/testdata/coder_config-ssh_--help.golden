Usage: coder config-ssh

Add an SSH Host entry for your workspaces "ssh coder.workspace"

- You can use -o (or --ssh-option) so set SSH options to be used for all your 
    workspaces.:                                                                

      $ coder config-ssh -o ForwardAgent=yes 

  - You can use --dry-run (or -n) to see the changes that would be made.:       

      $ coder config-ssh --dry-run

Options
    --ssh-config-file, $CODER_SSH_CONFIG_FILE  (default: ~/.ssh/config) 
        Specifies the path to an SSH config.

    --ssh-option, -o, $CODER_SSH_CONFIG_OPTS 
        Specifies additional SSH options to embed in each host stanza.

    --dry-run, -n, $CODER_SSH_DRY_RUN 
        Perform a trial run with no changes made, showing a diff at the end.

    --use-previous-options, $CODER_SSH_USE_PREVIOUS_OPTIONS 
        Specifies whether or not to keep options from previous run of
        config-ssh.

    --ssh-host-prefix
        Override the default host prefix.

    --yes, -y
        Bypass prompts.

---
Run `coder --help` for a list of global options.
