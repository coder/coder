coder v0.0.0-devel

USAGE:
  coder tokens remove [flags] <name|id|token>

  Delete a token

  Aliases: delete, rm

  Delete a token. Use --expire to expire the token instead of deleting it,
  preserving the token record for audit purposes.

OPTIONS:
      --expire bool
          Expire the token instead of deleting it, preserving the token record
          for audit purposes.

———
Run `coder --help` for a list of global options.
