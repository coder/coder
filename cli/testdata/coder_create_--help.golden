Usage: coder create [name]

Create a workspace

[1mOptions[0m
    --parameter-file, $CODER_PARAMETER_FILE
        Specify a file path with parameter values.

    --rich-parameter-file, $CODER_RICH_PARAMETER_FILE
        Specify a file path with values for rich parameters defined in the
        template.

    --start-at, $CODER_WORKSPACE_START_AT
        Specify the workspace autostart schedule. Check `coder schedule start
        --help` for the syntax.

    --stop-after, $CODER_WORKSPACE_STOP_AFTER
        Specify a duration after which the workspace should shut down (e.g. 8h).

    --template, -t, $CODER_TEMPLATE_NAME
        Specify a template name.

    --yes, -y
        Bypass prompts.

---
Run `coder --help` for a list of global options.
