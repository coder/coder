coder v0.0.0-devel

USAGE:
  coder open

  Open a workspace

SUBCOMMANDS:
    cursor      Open a workspace in Cursor Desktop
    fleet       Open a workspace in JetBrains Fleet
    vscode      Open a workspace in VS Code Desktop
    windsurf    Open a workspace in Windsurf Desktop
    zed         Open a workspace in Zed

———
Run `coder --help` for a list of global options.
