coder v0.0.0-devel

USAGE:
  coder rename [flags] <workspace> <new name>

  Rename a workspace

OPTIONS:
  -y, --yes bool
          Bypass prompts.

———
Run `coder --help` for a list of global options.
