Usage: coder login <url>

Authenticate with Coder deployment


 
Options
 
    --first-user-email, $CODER_FIRST_USER_EMAIL 
    Specifies an email address to use if creating the first user for the deployment.

    --first-user-username, $CODER_FIRST_USER_USERNAME 
    Specifies a username to use if creating the first user for the deployment.

    --first-user-password, $CODER_FIRST_USER_PASSWORD 
    Specifies a password to use if creating the first user for the deployment.

    --first-user-trial, $CODER_FIRST_USER_TRIAL 
    Specifies whether a trial license should be provisioned for the Coder deployment
    or not.

