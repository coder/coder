Usage: coder scaletest

Run a scale test against the Coder API

[1mSubcommands[0m
    cleanup              Cleanup any orphaned scaletest resources
    create-workspaces    Creates many users and a workspace for each one, then
                         waits for them to be ready

---
Run `coder --help` for a list of global options.
