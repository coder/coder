coder:
  image:
    tag: v2.30.0
aigateway:
  coderService:
    scheme: http
  keySecret:
    name: ai-gateway-key
  listenerTLS:
    secretName: listener-tls
ingress:
  enable: true
  host: ai.example.com
