coder:
  image:
    tag: v2.36.0
aigateway:
  coderService:
    scheme: http
  keySecret:
    name: ai-gateway-key
  listenerTLS:
    name: listener-tls
ingress:
  enable: true
  host: ai.example.com
