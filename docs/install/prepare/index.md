# Prepare prerequisites

Preparation gathers the external pieces a deployment depends on, such as DNS records, TLS certificates, a database, a license key, and an identity provider application.
Several of these have long lead times because another team owns them, so start them early.

<children></children>
