/**
 * Space between the bars in the chart.
 */
export const barsSpacing = 20;

/**
 * Height of the XAxis
 */
export const XAxisHeight = 40;

/**
 * Side padding to prevent the bars from touching the sidebar border, enhancing
 * visual separation.
 */
export const contentSidePadding = 4;

/**
 * Column width for the XAxis
 */
export const columnWidth = 130;

